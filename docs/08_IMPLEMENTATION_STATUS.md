# 08 — 游戏实现状态 v0.4（线上自动验收通过，未正式冻结）

本文件是当前开发阶段、实际完成度与发布状态的唯一事实源。旧聊天、素材计划及历史报告不能覆盖当前状态。

## 当前结论（2026-09-06）

**已完成文档收敛、Issue 修正、生产环境自动验收、稳定截图与演示录像采集。真实手机 / 小红书容器验收仍缺证据；Issue #2 保持打开，本轮未创建正式 v0.4 Tag / Release，未投稿。**

本次开始时的运行代码基线：`bfc5dd86e17f8c97791d583b974f138df237acc9`。

通过本次生产验收与采集的确切候选提交：`2c921031aeb04aeb5573ad60fd3f49afaf614b9d`。之后的状态文档提交不是新的游戏实现，未来冻结不得不加核对地指向漂移后的 main。

PR #1 已于 2026-09-05 合并；`5d337968c23be70cfc932ac35b342f84c5199bd8` 是历史 QA / DAY 1 美术合并点，不是当前 main，不再执行转 Ready / 再次合并。

## 正式入口：已纠正仓库路径与公网路径混淆

正式 Worker：`https://worker.1106314996.workers.dev/`。

实际公网入口：`/`、`/index.html`、`/play.html`。

部署将仓库 `game/` 挂载在站点根目录，故 `game/app.js` 对应公网 `/app.js`。从仓库根目录本地启动静态服务时才使用 `/game/index.html`、`/game/play.html`。

本次实测旧 `/game/` 入口及资源返回 404；根目录的 13 个运行文件均与候选 SHA-256 一致，根页面也与 `game/index.html` 一致。已修正文档和测试映射，**未配置旧链接兼容跳转，也未读取或修改 Cloudflare 后台构建配置**。文件内容一致不等于已读取 Cloudflare 的构建提交元数据。

## 本次真实执行与证据

最新完整运行：[production-smoke #6](https://github.com/Lorenzo-Holmes/-/actions/runs/34042764591)，目标为上述候选提交。

对应静态检查：[static-qa #85](https://github.com/Lorenzo-Holmes/-/actions/runs/34042764639)，通过。

证据包：[artifact 9992201044](https://github.com/Lorenzo-Holmes/-/actions/runs/34042764591/artifacts/9992201044)，SHA-256：`a52f4dcb869a2aa1eb55c588fbf835442db0ff574bab73fd28242b90de9ec594`。

| 验证范围 | 本次结果 |
| --- | --- |
| HTTP、MIME、运行文件内容 | 14 / 14 通过：根入口加 13 个运行文件 |
| Chromium 入口 / 场景 | 5 / 5 通过：根入口加两个入口 × 两种尺寸 |
| 手机尺寸 | 360×640、390×844 |
| 场景内流程 | 每个完整场景均通过 8 组检查 |
| Console、运行时异常、失败资源请求 | 四个完整场景的错误列表均为空 |
| 线上稳定截图和演示采集 | 通过；采集错误列表为空 |

每个完整场景验证：首屏布局与七种食材、图片解码、零道菜不解锁、预扣 / 撤回 / 无效配方不消耗、75% 留牛奶与 ¥4.80、无刷新重玩、100% / ¥0 / 精算规划型、进入 DAY 2。

证据包包括 16 张 QA 截图、4 段 QA 录像，以及 `submission-capture/` 下的 5 张稳定截图、约 47.92 秒原始演示录像和 `capture.json`。演示通过真实线上 UI 展示错误分配、75% 结算、重玩和 100% 结算；不注入库存、不伪造结算。原始演示录像不是已剪辑的最终宣传片。

## 截图复核与测试修正记录

已使用本次下载的真实截图进行 AI 视觉复核：首屏目标、食材与操作区可见；补采的 75% / 100% 报告文字与按钮可读，未被奖励卡遮挡。此结论不等于真人玩家研究或真实手机 / 小红书容器验收。

保留失败及修正历史：

- run `34041848692` 与 `34041936788`：旧 `/game/` 公网路径 404；后者的根目录诊断定位正确挂载路径。没有改写成通过。
- run `34042263662`：自动点击等待开火按钮动画静止而超时。改为检查可见、可用和中心命中后发送真实触控；未禁用动画或直接调用游戏函数。
- run `34042478780`：线上自动流程首次完整通过。
- 自动流程中的即时结算截图捕捉到尚未淡出的奖励卡；新增独立采集脚本等待真实提示自然结束，再取得稳定截图。没有隐藏图层或修改运行 UI。
- run `34042764591`：流程重测及稳定截图 / 演示采集均通过。

## 已实现内容（本轮不修改）

八关、25 种食材、20 道菜；即期食材、累计烹饪时间、份数、热量四类规则；四格料理区、多重集合配方匹配；预扣、撤回、成功后一次性扣库存；点击 / Pointer Events 拖拽；菜谱高亮、机会成本、两步教学、约 800ms 烹饪反馈、菜单详情；结算、localStorage 进度及 DAY 8 完成态。

首次推进至少成功完成一道菜，不要求 100% 或三星；已解锁关卡不重锁；DAY 8 至少完成一道菜才进入 8 / 8 完成态。该规则覆盖 `06_LEVEL_PROGRESSION.md` 的旧“结算即可解锁”表述。

DAY 1 仍是四道菜争抢三个鸡蛋，教程不告诉玩家最优解。没有新增玩法、调整数值、改变库存 / 配方 / 关卡 / 三星条件或重绘素材。

## 美术与脚本状态

DAY 1 七种正式食材与 B03 四道菜已经接入：番茄炒蛋、蛋炒饭、牛奶蒸蛋、洋葱煎蛋。不要依据旧《冰箱清空计划设计》记录重新进入 B03。素材来源和校验见 `12_DAY1_ASSET_MAPPING.md`。

当前仓库已存在 DAY 2 图集、`day3-8-food-assets.js` 与 `dish-render-hotfix.js`。它们不是本轮新增；文件存在不等于全八关正式视觉验收。实际脚本顺序以 `game/index.html` 为准：`app.js`、`enhancements.js`、`day3-8-food-assets.js`、`formal-assets.js`、`dish-render-hotfix.js`。

## 历史验证（不是本次重测）

主 QA 36 / 36、推进规则 20 / 20、DAY 1 正式资产 38 / 38，分别见 `10_BROWSER_GATE_REPORT.md`、`11_PROGRESSION_GATE_REPORT.md`、`13_FORMAL_ASSET_GATE_REPORT.md`。

历史数值验证记录 8 / 8 三星核心路线可达；最终关核心路线为即期 6 / 6、38 / 40 分钟、7 / 7 份、1180 / 1250 kcal。本次 smoke 未重新遍历全八关，不把这些历史结果冒充本次执行。

## 本轮交付位置

- `AGENTS.md`、根 README、文档索引及 `game/README.md`：阶段、读取顺序和入口修正。
- `14_PRODUCTION_SMOKE_GATE.md`：生产验收与冻结规程。
- `15_SUBMISSION_PACKAGE.md`：投稿简介、封面文案、60–75 秒分镜、采集路线、Release Notes 草稿。
- `scripts/production-smoke.cjs`：生产资源 / 交互验收。
- `scripts/capture-submission.cjs`：稳定画面与原始演示采集。
- `.github/workflows/production-smoke.yml`：只读权限的自动执行及证据上传；不会部署、打 Tag 或关闭 Issue。

## 执行环境与剩余工作

DevSpace `open_workspace` 返回 `FORBIDDEN: This conversation does not support developer MCPs`，未建立用户本机工作区。已改用连接的 GitHub 执行；本会话直接网页 / 容器访问受限，但 GitHub Actions 实际访问生产站点并取得上述证据，因此不能再把线上验收整体称为无法执行。

剩余发布确认：真实手机的点击 / 拖拽，以及实际小红书容器兼容性。浏览器的手机尺寸模拟不替代这些证据。取得现场结果后关闭 Issue #2，再将确切验收提交固定为正式 v0.4。尚未完成最终宣传片剪辑、实际投稿要求核对或投稿。

v0.5 在正式冻结后再评估现有 DAY 2–8 视觉一致性，不重复开发已经存在的内容，不在本轮增加新系统。
