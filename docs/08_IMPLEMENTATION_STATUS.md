# 08 — 游戏实现状态 v0.3（真实试玩 QA 修复阶段）

## 当前状态

**8 关核心玩法可用；真实试玩 QA 的 Batch A–C 已完成，本地 Chromium 主 Gate 36 / 36 通过，首次推进规则 Gate 20 / 20 通过。当前剩余阻塞已收敛为正式 PNG 资产与最终部署入口 smoke test。**

稳定试玩入口：[`../game/play.html`](../game/play.html)

运行版拆分为：

- `../game/index.html`：页面壳。
- `../game/styles.css`：基础视觉与移动适配。
- `../game/app.js`：数据、规则与核心交互。
- `../game/enhancements.css`：烹饪奖励 / 完成态 / 推进提示样式。
- `../game/enhancements.js`：奖励反馈、菜单详情、最终通关与首次推进规则。

详细记录：

- [`09_REAL_PLAY_QA_FIX_PLAN.md`](09_REAL_PLAY_QA_FIX_PLAN.md)
- [`10_BROWSER_GATE_REPORT.md`](10_BROWSER_GATE_REPORT.md)
- [`11_PROGRESSION_GATE_REPORT.md`](11_PROGRESSION_GATE_REPORT.md)

## 核心范围保持不变

- 8 个首发关卡。
- 25 种食材。
- 20 道首发菜品。
- 四类规则：即期食材、累计烹饪时间、总份数、总热量。
- 四格料理区与 multiset 配方匹配。
- 预扣 / 撤回 / 清空 / 开火后正式扣库存。
- localStorage 保存解锁、最好星级、教学完成与最终完成状态。

DAY 1 的“4 道鸡蛋菜争抢 3 个鸡蛋”结构没有改变。

## 推进规则

RISK-001 已按推荐方案实现并通过浏览器验证：

- **首次推进至少成功完成 1 道菜，才解锁下一关。**
- 不要求 100% 清空率。
- 不要求三星。
- 已经解锁的下一关不会因为回头重玩 0 道菜而重新锁住。
- DAY 8 至少完成 1 道菜后才写入 `P.completed=true` 并进入 8 / 8 完成态。

关卡页同步提示：`首次推进至少完成 1 道菜；三星不是门槛。`

浏览器回归：20 / 20 通过，详见 `11_PROGRESSION_GATE_REPORT.md`。

## 真实试玩后已修复

### 稳定性与结算

- 不再依赖 DOM ID 自动暴露到 `window` 绑定关键事件。
- `play.html` 不再用 iframe 点击拦截修补 `close/confirm`。
- 烹饪提交保持先验证、后一次性扣库存。
- 机会成本反馈会指出真正耗尽并关闭方案的资源。
- DAY 1 留牛奶路线恢复浪费 `¥4.80`。
- 非 100% 结算优先“重新规划”。
- DAY 1 100% 恢复“精算规划型 / 三个鸡蛋，一个都没白用 / 今晚 MVP：鸡蛋”。

### 核心交互

- 点击菜谱只高亮对应食材，不自动替玩家选择。
- 食材支持 Pointer Events 拖拽，同时保留点击快速加入。
- DAY 1 首次进入恢复两步教学，成功第一次开火后不再重复。
- 主要触控区约 44×44px 或更大。
- 短屏 / 窄屏使用 `100svh` 弹性布局和可换行即期目标。

### 奖励与完整性

- READY 后开火增加约 800ms 轻量烹饪反馈，并在期间锁定关键操作。
- 完成菜品出现短奖励卡。
- “今晚菜单”显示缩略条，点击完成菜可查看人份、时间、kcal。
- DAY 8 满足首次完成门槛后进入 `8 / 8` 完成首页，并显示最好总星数。

## 浏览器 Gate

2026-09-05 已完成两组 Chromium 交互回归：

### 主 QA Gate

**36 / 36 通过，0 失败。**

覆盖：

- 两步教学。
- 44px 关键 Hitbox。
- 菜谱高亮但不自动选择。
- 真实鼠标拖拽只加入一次食材。
- 800ms 烹饪期间不能撤回 / 重复开火。
- 菜品奖励卡与详情。
- DAY 1 75% / ¥4.80。
- DAY 1 100% / ¥0 / 鸡蛋 MVP。
- DAY 8 完成首页。
- 360×640、375×667、390×844、430×844 核心操作首屏可见，无横向溢出。
- 全流程无捕获到的运行时 JavaScript Error / unhandled rejection。

### 首次推进规则 Gate

**20 / 20 通过，0 失败。**

覆盖：

- 新玩家 0 道菜不解锁下一关。
- 完成任意 1 道菜即可首次推进。
- 已解锁关卡重玩 0 道菜不重新锁回。
- DAY 8 0 道菜不进入完成态。
- DAY 8 完成 1 道菜后进入 8 / 8。

当前执行环境的组织策略阻止浏览器直接导航 `http://`、`file://` 与 `data:` URL，因此 Gate 使用 Chromium `Page.setDocumentContent` 加载当前分支等价完整页面字节完成。最终部署 URL 的两个入口仍需做一次 smoke test。

## 视觉资产状态

此前已经验收通过的 DAY 1 正式 PNG 原文件从未进入当前 Git 仓库，目前无法从 Git 历史恢复。

已确认：

- 当前 Git tree 没有 PNG / JPG / WebP。
- 仓库历史没有美术资产提交。
- 当前运行环境没有此前验收通过的原 PNG。

代码已准备：

- DAY 1 固定空间摆位。
- `Ingredient.asset` 图片接口。
- `DISH_ASSETS` 菜品图片接口。
- 数量、“今天”、预扣、高亮继续由 UI 动态叠加。

资产落位目录已经建立：

- `game/assets/ingredients/day1/`
- `game/assets/dishes/day1/`
- `game/assets/scene/day1/`

各目录 README 已固定建议文件名与接入要求。

在原文件补回前，运行版继续使用 Emoji / 食材组合 fallback；这只是功能占位，不是最终美术。

## 关卡数值验证

原 8 关三星核心路线的程序化验证仍保持：8 / 8 路线可达三星。

最终关核心路线：

- 即期食材：6 / 6；
- 时间：38 / 40 分钟；
- 份数：7 / 7；
- 热量：1180 / 1250 kcal。

本轮没有修改这些关卡库存 / 菜谱数值。

## 当前剩余 Gate

在改为“比赛版完成”之前只剩：

1. 从原生成会话或本地备份找回已经验收的 DAY 1 正式食材 / 菜品 PNG。
2. 将原文件放入既定 `game/assets/...` 目录，并填写 `Ingredient.asset` / `DISH_ASSETS`。
3. 接入正式图片后再跑一次 360×640 与 390×844 视觉 Gate。
4. 正式部署后 smoke test `game/index.html` 与 `game/play.html`，确认 Console 无阻断错误。

资产恢复阻塞记录在 Issue #2。

## 下一步

不要继续新增商城、养成、AI 菜谱、联机、排行榜等系统。

下一步只应执行：**恢复正式 PNG → 接入 → 正式图片移动端 Gate → 部署入口 smoke test → PR 转 Ready / 合并。**
