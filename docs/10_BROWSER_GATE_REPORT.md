# 10 — 真实浏览器 Gate 报告

## 结论

2026-09-05 对 `fix/real-play-batch-a` 当前实现进行了本地 Chromium 真实交互回归。

结果：**36 项通过，0 项失败。**

本次验证覆盖 Batch A、Batch B、Batch C 的核心交互与主要移动端尺寸，不改变任何关卡库存、菜谱或三星条件。

## 验证环境

- Chromium 144 Headless。
- 页面内容使用当前分支 `game/index.html + styles.css + app.js + enhancements.css + enhancements.js` 的等价完整内容组装。
- 使用 Chrome DevTools Protocol 驱动真实鼠标事件和 DOM 点击。
- 测试环境的组织策略会拦截 `http://`、`file://` 和 `data:` 直接导航，因此通过 `Page.setDocumentContent` 将完全相同的页面字节写入 `about:blank` 文档进行交互验证。
- 因此本报告可以验证页面逻辑、布局、Pointer Events 和运行时错误，但不能替代最终部署 URL 对 `game/index.html` / `game/play.html` 两个入口的最后一次 smoke test。

## 通过项

### 启动与运行时

- 页面正常渲染。
- 全流程无捕获到的运行时 JavaScript Error / unhandled rejection。
- 不再依赖 DOM ID 自动成为 `window` 全局变量。

### 44×44 触控区

以下控件实际高度均达到 44px：

- `结束 / 完成今晚`
- `还能做`
- `清空`
- `开火`
- 菜谱抽屉关闭按钮

料理槽删除按钮也在此前 CSS Gate 中维持 44×44 点击区。

### QA-003 菜谱高亮

- 点击“番茄炒蛋”后实际高亮番茄与鸡蛋两个库存食材。
- 高亮行为不会向料理槽加入任何食材。
- 不修改库存。

### QA-004 拖拽

使用 Chromium 真实鼠标按下、移动、释放事件，从鸡蛋拖到料理槽：

- 料理槽只加入 1 个鸡蛋。
- 没有出现 pointerup 后再触发 click 导致双重加入。
- 点击输入仍与拖拽输入共存。

### QA-005 两步教学

首次 DAY 1：

1. 显示 `点击或拖动食材，把它放进料理区。`
2. 第一次组成 READY 配方后显示 `配方凑齐了，开火试试。`

第一次成功开火后：

- `tutorial.basicDone=true` 持久化。
- 重新规划 DAY 1 不再重复强制教学。

### QA-011 烹饪锁定与奖励反馈

点击 READY 的“开火”后：

- `S.cooking=true`。
- 开火按钮禁用并显示烹饪状态。
- 料理槽 `pointer-events:none`。
- 烹饪期间用真实鼠标点击槽位删除按钮，选择内容不会变化。
- 约 800ms 后事务正常提交。
- 完成菜进入今晚菜单。
- 菜品奖励卡成功创建。
- 点击已完成“番茄炒蛋”可打开详情，并正确显示 `2 人份 / 8 分钟 / 280 kcal`。

### DAY 1 — 75% 路线

路径：

`洋葱煎蛋 → 番茄炒蛋 → 蛋炒饭`

浏览器结果：

- 清空率：75%。
- 浪费：¥4.80。
- 菜品：3 道。
- 星级：★★☆。
- 明确显示剩余 `牛奶×1`。
- `重新规划` 为 primary CTA。

### DAY 1 — 100% 路线

路径：

`番茄炒蛋 → 蛋炒饭 → 牛奶蒸蛋`

浏览器结果：

- 清空率：100%。
- 浪费：¥0.00。
- 菜品：3 道。
- 营养：A。
- 星级：★★★。
- 人格：`精算规划型`。
- 文案：`三个鸡蛋，一个都没白用。`
- `今晚 MVP：鸡蛋`。
- `下一关` 为 primary CTA。

### QA-012 DAY 8 完成态

按当前既有“完成结算即可推进”规则完成 DAY 8 结算后：

- 最终按钮为 `完成计划`。
- 回到首页显示 `8 / 8`。
- 显示 `冰箱清空计划完成`。
- 显示最好总星数 / 24。
- `查看关卡` 与 `重玩 DAY 8` 均存在。

RISK-001 未在本次测试中修改；0 道菜是否应该允许首次推进仍是独立产品决策。

## 移动端尺寸 Gate

以下尺寸均通过“开火按钮首屏完整可见、顶部即期区域未裁切、无横向溢出”的自动测量：

| 尺寸 | 开火按钮 bottom | viewport 高度 | 即期区域 | 横向溢出 |
|---|---:|---:|---|---|
| 360×640 | 635 | 640 | 未裁切 | 无 |
| 375×667 | 662 | 667 | 未裁切 | 无 |
| 390×844 | 836 | 844 | 未裁切 | 无 |
| 430×844 | 836 | 844 | 未裁切 | 无 |

360×640 的开火按钮底部距离 viewport 底部约 5px，属于可操作但较紧的边界状态；后续正式资产接入后应再次复核图片尺寸是否影响这一结果。

## 未完成 Gate

### 1. 正式美术资产

当前 Git 树与当前运行环境均不存在此前验收通过的 DAY 1 PNG 原文件。

仍需补入：

- DAY 1 七种正式食材 PNG。
- DAY 1 四道正式菜品 PNG。
- 如正式冰箱/锅资产也计划进入运行版，则同时补入。

代码中的 `Ingredient.asset` 与 `DISH_ASSETS` 已预留，不需要再改交互逻辑。

### 2. 部署入口 smoke test

因为当前 Chromium 的组织策略阻止直接访问 `http://`、`file://`、`data:` URL，本环境无法对最终部署 URL 做入口级验证。

正式部署后只需再做一次快速检查：

- `game/index.html` 可直接进入。
- `game/play.html` iframe 入口可进入。
- 两入口无 Console 阻断错误。
- 360×640 再跑一次首屏检查。

## 当前判断

真实试玩 QA 的代码层和本地浏览器交互层已经通过。

在正式 PNG 补入、部署入口 smoke test 完成之前，PR 继续保持 Draft；不应继续新增商城、养成、排行榜或其他非核心系统。
