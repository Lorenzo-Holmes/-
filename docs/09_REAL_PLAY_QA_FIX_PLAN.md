# 09 — 真实试玩 QA 修复计划与执行状态

## 目标

本轮依据真实试玩问题清单修复比赛版核心体验，不改变 DAY 1 的“4 道鸡蛋菜争抢 3 个鸡蛋”结构，也不扩展商城、养成、排行榜、AI 菜谱等非核心系统。

当前执行分支：`fix/real-play-batch-a`。

## Batch A — 可发布基础

状态：**首轮实现完成，待真实浏览器 Gate。**

已完成：

- QA-001：移除 DOM ID 自动暴露到 `window` 的事件绑定依赖；`play.html` 不再承担 `close/confirm` 行为补丁。
- QA-002：`cook()` 改为事务式库存提交，并识别真正导致菜谱关闭的耗尽资源。
- QA-006：DAY 1 恢复浪费金额、非满分“重新规划”主 CTA、100% 精算规划型与鸡蛋 MVP。
- QA-007：所有食材增加名称 fallback，白菜/生菜即使共用 Emoji 也能直接区分。
- QA-008：游戏改为 `100svh` 弹性布局，即期 Chip 可换行，并增加短屏规则。
- QA-009：返回、结束、还能做、抽屉关闭、槽位删除、烹饪与 Modal 操作的 Hitbox 至少约 44×44px。

DAY 2–8 的逐食材 `wasteValue` 在现有规范中没有完整正式数据，因此没有自行编造；无法计算时结算金额显示 `—`。

## Batch B — 核心交互

状态：**代码实现完成；QA-010 正式 PNG 文件仍待接入。**

### QA-003 菜谱点击高亮

已完成：

- 菜谱项改为可点击按钮。
- 点击未完成菜谱后只高亮该菜谱当前仍在库存中的对应食材。
- 抽屉自动收起。
- 高亮约 800ms 后自动解除。
- 不自动加入料理槽、不扣库存、不自动开火。

### QA-004 Pointer Events 拖拽

已完成：

- 食材支持 `pointerdown / pointermove / pointerup / pointercancel`。
- 8px 位移阈值后才进入拖拽，避免短按误判。
- 拖拽时显示 Ghost，料理槽区域进入可投放高亮。
- 放到料理槽调用与点击相同的 `addIngredient()`。
- 放到空白区域不修改库存/选择。
- 拖拽完成后抑制紧随其后的 click，避免一次操作重复加入。
- 点击快速加入仍完整保留。

### QA-005 首次两步教学

已完成：

- DAY 1 首次进入显示：`点击或拖动食材，把它放进料理区。`
- 第一次加入食材后第一步结束。
- 第一次形成 READY 配方时显示：`配方凑齐了，开火试试。`
- 第一次成功开火后写入 `P.tutorial.basicDone`。
- 之后重新规划 DAY 1 不再强制重播两步教学。
- 教学没有提示“鸡蛋只有三个”、最优三道菜或洋葱煎蛋是错误选择。

### QA-010 DAY 1 正式资产接入

已完成基础接入层：

- DAY 1 改为按正式规范固定空间摆位，不再依赖库存对象顺序套 `p0/p1/...`。
- 已固定牛奶、鸡蛋、午餐肉、剩米饭、番茄、胡萝卜、洋葱的位置与尺寸。
- `Ingredient` 数据增加可选 `asset` 字段。
- 渲染器支持存在 `asset` 时使用图片，不存在时保留 Emoji + 名称 fallback。
- 数量、“今天”、预扣和菜谱高亮继续由 UI 动态叠加。

当前缺口：此前验收通过的 DAY 1 正式 PNG 原文件没有进入 GitHub 仓库，当前工作环境也只有合成图文件名记录，没有原资产本体。因此没有重新生成或伪造替代图片；`asset` 目前保持 `null`。正式 PNG 一旦补入仓库，只需填写路径即可切换，不需要再改交互逻辑。

## Batch C — 奖励反馈与完整性

状态：**代码实现完成；正式菜品图片待补。**

### QA-011 做菜奖励反馈

已完成：

- READY 后点击开火进入约 800ms `cooking` 状态。
- 烹饪期间锁定冰箱、菜单、料理槽、清空与结束等交互，避免事务中途被修改。
- 锅体加入轻量摇动 / 发光反馈，不增加复杂烹饪小游戏。
- 约 800ms 后才重新验证 READY 与库存并一次性提交，库存事务规则保持不变。
- 完成后显示约 700ms 的菜品奖励卡。
- “今晚菜单”升级为缩略条 + 菜名。
- 点击已完成菜品可查看菜名、人份、时间、kcal。
- 菜品视觉增加 `DISH_ASSETS` 接口；正式菜品 PNG 不存在时，使用对应食材 Emoji 组合作为临时缩略图，不冒充正式美术。

### QA-012 DAY 8 最终完成状态

已完成：

- DAY 8 结算后写入 `P.completed=true`。
- DAY 8 结果页最终按钮改为 `完成计划`。
- 返回首页后显示 `冰箱清空计划完成` 与 `8 / 8`。
- 完成首页显示当前最好总星数 / 24。
- 主按钮为 `查看关卡`，次按钮为 `重玩 DAY 8`。
- 重置进度仍可用。

### RISK-001

未修改规则。

当前仍保持“完成结算即可解锁下一关”的既有逻辑，没有擅自加入“至少做 1 道菜”门槛。该项仍需产品层明确决策。

## 结构调整

运行版已由单一 `game/index.html` 拆为：

- `game/index.html`：页面壳与资源加载顺序。
- `game/styles.css`：基础视觉、移动端、拖拽和教学样式。
- `game/app.js`：数据、状态、规则与核心交互。
- `game/enhancements.css`：烹饪奖励与完成态样式。
- `game/enhancements.js`：Batch C 奖励、菜单详情与最终通关闭环。

`game/play.html` 继续作为兼容试玩入口，通过 iframe 加载 `index.html`。

## 自动检查

已完成：

- 本地 Batch B `app.js` 通过 `node --check`。
- 本地 Batch C `enhancements.js` 通过 `node --check`。
- DAY 1 Route A 规则模拟：番茄炒蛋 → 蛋炒饭 → 牛奶蒸蛋，100%，浪费 ¥0。
- DAY 1 Route B 规则模拟：洋葱煎蛋 → 番茄炒蛋 → 蛋炒饭，75%，剩余牛奶×1，浪费 ¥4.80。
- 最后一个鸡蛋耗尽时，因果提示只点名真正造成方案关闭的 `鸡蛋`。

未宣称通过：当前执行环境无法稳定完成完整移动浏览器自动交互，因此下列真实浏览器 Gate 仍需在部署/可交互浏览器中复测。

## 待真实浏览器 Gate

- `game/index.html` 与 `game/play.html` 两入口关键按钮。
- 360×640 / 375×667 / 390×844 / 430 宽布局。
- 点击与拖拽是否都只加入一次食材。
- 菜谱点击高亮后库存和料理槽保持不变。
- 两步教学首次出现且重新规划后不重复。
- 800ms 烹饪过程中不能撤回 / 重复开火。
- 完成菜品奖励卡与“今晚菜单”详情可正常打开/关闭。
- DAY 1 75% / 100% 两条完整结算流程。
- DAY 8 结算后出现 8/8 完成首页。
- Console 无阻断错误。

## 当前未完成项

1. 将此前已经验收通过的 DAY 1 正式食材 PNG 原文件补入仓库并填写 `Ingredient.asset`。
2. 将正式 DAY 1 菜品 PNG 补入仓库并填写 `DISH_ASSETS`。
3. 完成真实移动浏览器 / 部署环境回归。
4. 产品层决定 RISK-001 是否增加“至少完成 1 道菜”推进门槛。

## 当前 Gate

功能修复已经覆盖 Batch A、Batch B、Batch C。当前不应继续新增系统；下一步应转入真实浏览器复测、正式资产补齐，以及根据复测结果做小范围修正。
